<!--# Using a version of Python < 1.5 -->
{% load url from future %}
<!-- {% load staticfiles %} -->
<!-- <link rel="stylesheet" href="{% static stylesheet %}" type="text/css" /> -->
<!-- <link rel="stylesheet" href="css/stylesheet.css" type="text/css" /> -->
<link rel="stylesheet" href="{{ STATIC_URL }}stylesheet.css" type="text/css" />

<h1> Users </h1>

{% if latest_user_list %}
    <h3> List of the users </h3>
    <ul>
        {% for user in latest_user_list %}
            <li><a href="{% url 'detail' user.id %}">{{ user.username }}</a></li>
        {% endfor %}
    </ul>
{% else %}
    <h3> No users available. </h3>
    <p> And no functionality to make them! What a pickle.</p>
{% endif %}
